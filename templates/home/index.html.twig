{# templates/home/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>
    <p>{{ subtitle }}</p>

    <p>Zapraszamy do przeglądania naszych produktów!</p>
    <a class="btn btn-primary" href="{{ path('app_products_list') }}">Zobacz produkty</a>
{% endblock %}
